{
  "name": "@singularity/plugin-release-source-checkout",
  "description": "The private, detached git checkout a release of committed code builds from: acquire one pinned to a commit (held by a kernel flock for the owning process's life), dispose of it, and sweep the checkouts whose owner died.",
  "private": true,
  "version": "0.0.1"
}
