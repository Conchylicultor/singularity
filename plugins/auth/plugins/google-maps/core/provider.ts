/**
 * The provider id, named once. The central descriptor, the Accounts row, the
 * setup wizard and the integrations broker all read it from here so they
 * cannot drift onto different spellings of the same account.
 */
export const GOOGLE_MAPS_PROVIDER_ID = "google-maps";
