// Shared between web and server: the category config-conflict tasks are filed
// under. The server contributes the matching TaskCategory registration; the web
// half names it as the draft popover's target.
export const CONFIG_CATEGORY_ID = "config";
