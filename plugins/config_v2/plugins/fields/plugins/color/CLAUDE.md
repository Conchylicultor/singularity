# color

Color field type for config_v2. Stores a hex color string (e.g. `"#ff0000"`).
The web renderer wraps `ColorPickerPopover` from the `color-picker` primitive:
a popover with color area, hue slider, optional alpha slider, optional swatches,
and a hex input.

## Usage

```ts
import { colorField } from "@plugins/config_v2/plugins/fields/plugins/color/core";

const myConfig = defineConfig("my-plugin", {
  fields: {
    accent: colorField({
      label: "Accent color",
      description: "Primary accent color for the UI",
      default: "#3b82f6",
      swatches: ["#ef4444", "#3b82f6", "#22c55e", "#f59e0b"],
    }),
    highlight: colorField({
      label: "Highlight color",
      showAlpha: true,
    }),
  },
});
```

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Color field type: hex color string with a popover color picker.
- Exports (core):
  - Types: `ColorFieldDef`
  - Values: `colorField`, `colorFieldType`
- Contributes:
  - `Fields.Renderer` "color" → `ColorRenderer`

<!-- AUTOGENERATED:END -->
