# derived-updated-at

A table's `updated_at`, maintained by the database from a per-column
declaration instead of stamped by hand at every write site. Plan:
`research/2026-09-25-global-derived-updated-at.md`.

The declaration lives in `infra/entities` (`defineEntity`'s
`meta.updatedAt: { touchedBy }` — see its CLAUDE.md). This plugin is the part
the database plugin can import without a cycle (entities pulls in the fields
capability loader, whose barrels reach `@plugins/database/server`):

- `compileDerivedUpdatedAt({ table, updatedAtColumn, columns })` — pure. Turns
  each column's `TouchRule` into one BEFORE UPDATE row trigger:
  - `true` → `NEW.c IS DISTINCT FROM OLD.c`;
  - `{ into, outOf }` → `(NEW.c IS DISTINCT FROM OLD.c AND (NEW.c IN (into) OR OLD.c IN (outOf)))`
    (`null` as `IS NULL`; scalar values only, a jsonb column is refused);
  - `false` → nothing.

  The function sets `updated_at := now()` iff the OR of those holds (no counted
  column ⇒ it never moves after insert), and RAISEs when a write changes
  `updated_at` itself — rewriting its own value is not a change and passes.
  INSERT is untouched (`defaultNow()`).
- `registerDerivedUpdatedAt(spec)` — the module registry `defineEntity` fills at
  module eval; the same table declared twice with different rules throws.
- `installDerivedUpdatedAt(db, specs?)` — called by `database/server`'s
  `onReadyBlocking` right after `runMigrations`. Follows the
  `jobs/superseded-trigger.ts` precedent: the DDL's sha256 is the trigger's
  COMMENT, so an up-to-date trigger is a catalog-only no-op; otherwise, per
  table, `pg_advisory_xact_lock` + re-check + `CREATE OR REPLACE FUNCTION` /
  `TRIGGER` + COMMENT in one transaction. Then it asserts every trigger is
  present with its signature, and throws if not. A missing table throws.

  `specs` defaults to the whole registry. The registry is process-wide, so a DB
  test on a throwaway database passes exactly its own entity's
  `entity.derivedUpdatedAt` spec rather than the registry (which holds every
  other suite's entities too).

Tests: `server/internal/compile.test.ts` (compiler output); the real-DB suite
that drives the trigger end to end is
`infra/entities/server/internal/install-derived-updated-at.test.ts`.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Derived updatedAt: compiles a table's per-column touchedBy rules (declared in defineEntity's meta.updatedAt) into a BEFORE UPDATE trigger that sets updated_at = now() only when a counted column really changed and RAISEs on any app write to it; a registry filled at module eval, and the boot installer (signature-in-COMMENT, advisory-locked, asserted) the database plugin runs right after migrations.
- Cross-plugin:
  - Imported by:
    - `database`
    - `infra/entities`
- Server:
  - Exports (types):
    - `DerivedUpdatedAtSpec`
    - `TouchRule`
  - Exports (values):
    - `compileDerivedUpdatedAt`
    - `installDerivedUpdatedAt`
    - `registerDerivedUpdatedAt`

<!-- AUTOGENERATED:END -->
