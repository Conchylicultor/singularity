# zero

> ## ⚠️ FROZEN PILOT — no further investment
>
> This subtree (the Rocicorp Zero sync-engine pilot: `cache-service`, `client`,
> and the `debug/zero-test` harness that consumes it) is **frozen**. It is a
> parked exploration of the vision's **Axis B** (move reads client-side for 0ms
> interaction — see
> [`research/2026-06-21-global-live-state-ivm-and-instant-client-vision.md`](../../../../research/2026-06-21-global-live-state-ivm-and-instant-client-vision.md)),
> not a shipping path.
>
> **The committed direction is the in-house live-state stack** (`resource-runtime`
> + `live-state` + the L1–L4 layers), and specifically its **Axis A** declarative
> query-resource work, per
> [`research/2026-07-02-global-comms-structural-fixes.md`](../../../../research/2026-07-02-global-comms-structural-fixes.md)
> (Track 2 recorded this freeze). Do **not** add features here, migrate real
> reads onto `useZeroResource`, or grow the pilot schema. Keep it building; leave
> it inert.
>
> ### The fence (kept behind `SINGULARITY_ZERO_CACHE`, default OFF)
>
> A single explicit opt-in switch gates the entire feature — presence of the
> `@rocicorp/zero` dependency does **not** (it is always committed for the client
> bundle, so a presence-gate would silently auto-start the sidecar for everyone):
>
> - `zeroCacheEnabled()` reads `process.env.SINGULARITY_ZERO_CACHE === "1"`
>   (`plugins/infra/plugins/launcher/server/internal/boot.ts`). Consulted once,
>   by `zeroCacheSpec()`. With the flag unset, the worktree `spec.json` carries
>   **no** `zeroCache` block and is byte-identical to a pre-Zero spec.
> - The gateway spawns the Node `zero-cache` sidecar **lazily on the first
>   `/zero/*` request, and only if `spec.json` has a `zeroCache` block**
>   (`gateway/worktree.go` `EnsureZeroCache` → `ErrZeroCacheDisabled`). With the
>   flag off it never spawns; `/zero/*` **404s** immediately
>   (`gateway/proxy.go`).
>
> ### Zero-cost-when-disabled: verified, with three characterized residuals
>
> With the flag off there is **no sidecar process, no `/zero/*` routing, and no
> `zero%` replication slot ever created**, so per-worktree runtime cost is zero.
> The residual costs are bounded and understood:
>
> 1. **Slot-sweep job tick** — `database.zero-slot-sweep` (`cache-service`) runs
>    every 5 min on the **main** runtime regardless of the flag. This is
>    *intended* (the super-plan sanctions "no slot-sweep churn beyond the no-op
>    job tick"): it must run unconditionally to reclaim slots orphaned if the
>    flag was ever toggled on. With the flag off it matches 0 rows — one cheap
>    `pg_replication_slots` query, no work.
> 2. **Per-reap cleanup** — `debug/worktree-cleanup` calls
>    `dropZeroReplicationArtifacts(id)` on every worktree reap, unconditionally.
>    Also *intended defensive cleanup* (must survive flag toggles): two
>    idempotent DDL-check queries + one `fs.rm` of a replica path that never
>    exists. Negligible, correct.
> 3. **First-install provisioning** — the `cache-service` postinstall provision
>    (`ensureZeroSqlite3` + `ensureZeroNode`) builds a native addon and downloads
>    a Node 24 tarball, **not** gated on the flag (only on filesystem
>    idempotency). This is the one genuine build-time coupling: a one-time
>    ~35 MB + native-build cost on a fresh machine even when Zero is disabled.
>    **Follow-up filed** to gate provisioning on `SINGULARITY_ZERO_CACHE` so the
>    fence is complete at install time too. (Residuals 1–2 are deliberately left
>    unconditional — gating them would leak orphaned slots across a flag toggle.)
>
> ### Deliberately deferred (do NOT remove)
>
> The embedded cluster runs with `wal_level=logical` + `listen_addresses=127.0.0.1`
> (`plugins/database/plugins/embedded/scripts/start.ts`) to make it
> logical-replication-consumable. These are **postmaster-start-only** GUCs; they
> impose ~no steady-state cost and removing them would only complicate a future
> un-freeze. Keep them.
>
> ### Re-evaluation trigger
>
> Un-freeze **only if/when the vision's Axis B (a committed client-side store)
> becomes a scheduled track** — at which point Zero is re-compared against
> building B1–B3 on the in-house delta transport. Absent that, this stays parked.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Umbrella for the Rocicorp Zero sync-engine infrastructure: shared constants (core), the zero-cache supervised service (cache-service), and the generic client provider + adapter (client). Domain-agnostic — no concrete schema.
- Core:
  - Exports: Values: `ZERO_CACHE_PORT`
- Sub-plugins:
  - **`cache-service`** — zero-cache sidecar service: the supervised Node process that replicates the main Postgres DB into Zero's SQLite replica. Schema-agnostic.
  - **`client`** — Generic, schema-parameterized Zero client: the ZeroRoot provider wrapper, the useZeroResource (ResourceResult-shaped) adapter, and a raw useZeroQuery re-export. No concrete schema.

<!-- AUTOGENERATED:END -->
