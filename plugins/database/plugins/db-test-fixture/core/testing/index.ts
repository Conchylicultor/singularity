// Mints a name in the throwaway-database grammar, so a suite can check how a
// name the fixture would create is classified without creating one.
export { mintTestDbName } from "../internal/scratch-name";
