# relocate

`./singularity plugin move <from> <to> [--dry-run]` — moves or renames a plugin
and every descendant in one step. Design:
[`research/2026-09-23-global-plugin-relocation.md`](../../../../research/2026-09-23-global-plugin-relocation.md).

## What it does

1. **Refuses** in the main checkout, on a dirty tree (`--dry-run` only notes
   it), when `<from>` is not a plugin, `<to>` exists, `<to>` sits inside
   `<from>`, `<to>`'s parent is neither `plugins/` nor an existing plugin, or
   `config/<to slash path>` exists.
2. **Locates before moving**: `findPluginRefs` (plugin-refs) over the repo file
   set. Refs carry old paths and old offsets, so nothing is read after the move.
3. **Plans** (`cli/internal/plan.ts`, pure, unit-tested): for each ref,
   - path ref inside the moved subtree → swap the dir prefix;
   - dot ref inside it → swap the id prefix;
   - relative ref whose source OR target moves → resolve against the source's
     old location, map the target through the move, re-relativize from the
     source's new location (`./` kept where it was written, and always for CSS
     — a bare `@import` is a package).

   Edits apply back-to-front per file; overlapping ranges or a range that no
   longer holds its value throw. `*.generated.ts` and `bun.lock` are never
   edited (a build rewrites them); in `research/` only links are.
4. **Applies**: `git mv` the plugin dir and `config/<old slash path>` (if
   tracked), writes each rewritten file at its new path, and re-derives every
   moved `package.json` `"name"` (`packageNameFor` + codegen's
   `withPackageName`) so no install sees a stale or duplicate workspace name.
5. **Reports**: per-kind edit counts and per-file edits; `@source` globs that
   covered the old location and will not cover the new one; moved files that
   compute paths from their own location (`import.meta.dir` / `__dirname`) when
   the depth changes; user-layer config dirs left under the old slash path in
   every namespace (not migrated); the `plugin_health_reviews` ids that orphan.

Then: `./singularity build` (registries, docs, CLAUDE.md autogen blocks,
`bun.lock`), then `./singularity check` — the same checks validate the refs the
mover rewrote, so anything it missed fails there.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: `./singularity plugin move <from> <to>` — relocate or rename a plugin (and every descendant) in one step: git mv the folder and its config dir, then rewrite every reference the plugin-refs locator finds (path literals, @plugins specifiers, dot ids, relative links) by exact range.

<!-- AUTOGENERATED:END -->
