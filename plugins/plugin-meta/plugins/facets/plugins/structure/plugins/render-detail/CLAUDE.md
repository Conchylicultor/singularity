# render-detail

Per-plugin structure section in the plugin detail pane. Contributes a
`PluginView.Section` (id `structure`) that reads `node.facets["structure"]`
and renders a flag row: an info badge for a self-declared composition root and
warning badges for each non-standard folder and stray top-level source file.
A structurally conformant plugin (no anomalies) is gated out by the
contribution's `useAvailable`, so the host paints no card at all.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Per-plugin structure section in the plugin detail pane.
- Web:
  - Contributes: `PluginViewSlots.Section` "Structure" → `StructureDetailSection`
  - Uses:
    - `plugin-meta/plugin-view.PluginViewSlots`
    - `primitives/css/badge.Badge`
    - `primitives/css/spacing.Stack`

<!-- AUTOGENERATED:END -->
