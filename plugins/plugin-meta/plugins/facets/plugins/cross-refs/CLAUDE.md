# cross-refs

Extracts cross-plugin import relationships: per-runtime `apiUses` arrays
(server/central/web/core/shared) and the `importedBy` reverse index.

Uses `extract()` to scan each plugin's runtime directories for ES import
statements referencing other plugins' barrels. Uses `relate()` to invert the
apiUses graph into per-target `importedBy` lists.

The exported `crossRefsFacetDef` token lets other facets (e.g. `exports`)
read this facet's data via `getFacet(node, crossRefsFacetDef)` during their
own `relate()` pass.

Browser rendering lives in the `render-diff` / `render-detail` / `render-catalog`
sub-plugins, each reading `node.facets["cross-refs"]` and contributing to an
existing web slot. The diff projection (`crossRefsToComparable`, the deduped
`apiUses` union) lives in `core/`.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Core:
  - Uses: `framework/plugin-id.asPath`, `framework/plugin-id.RUNTIME_FOLDERS`, `plugin-meta/facets.defineFacet`
  - Exports: Types: `ApiUse`, `CrossRefsData`, `RawUse`; Values: `crossRefsFacetDef`, `crossRefsToComparable`
- Cross-plugin:
  - Imported by: `plugin-meta/closure`
- Sub-plugins:
  - **`render-contributions`** — Aggregated cross-plugin cross-refs table in the Studio Contributions view.
  - **`render-detail`** — Per-plugin cross-refs section in the plugin detail pane.
  - **`render-diff`** (disabled — cascade) — Diff renderer for the cross-refs facet (PR review).

<!-- AUTOGENERATED:END -->
