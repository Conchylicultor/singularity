import type { PluginDefinition } from "@core";

export default {
  id: "plugin-meta",
  name: "Plugin Meta",
  description:
    "Plugins about the plugin system itself — browsing, inspecting, and publishing.",
  contributions: [],
} satisfies PluginDefinition;
