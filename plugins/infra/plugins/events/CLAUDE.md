# events

## Dangling-binding reconciliation

A trigger binding is a **persisted DB row** (one per subscription, in the
event's `<event_name>_triggers` table), not in-memory state. So a binding can
outlive the code that created it — most dangerously when its target job is
deleted but the row survives. Every emission of that event then enqueues an
`events.dispatch` job that can never resolve a handler; pre-fix those threw and
graphile retried each into a dead job (the `improve.apply-queue-top` ×589
retry storm). Two independent layers now make that drift self-correcting, so a
stale binding can never re-accumulate dead jobs:

1. **Boot sweep** — `sweepStaleTriggers()` (in `onReady`) deletes, in one atomic
   `DELETE … RETURNING` per table, every trigger row whose `jobName` is not in
   the live job registry, and reports what it removed. It runs after the
   register phase, so the registry is complete (an empty-registry guard prevents
   wiping valid triggers if ever called too early).
2. **Dispatch self-heal** — `events.dispatch` (`dispatch-job.ts`) resolves the
   target by name; an unknown job means the binding is dead, so it removes that
   trigger row and returns without throwing (no retry, no dead job). This covers
   the window between a job's removal and the next boot sweep.

Static triggers are declared as `Trigger({ on, do, with })` contributions and
re-projected from code each boot by `syncTriggerContributions()` (wipe + insert
per declared job), so the DB is a pure projection of code — removing a job's
declaration cannot leave a stale row. Trigger **tables** are real Drizzle tables
built inside `defineTriggerEvent`; deleting an event's source file makes
drizzle-kit generate a `DROP` migration on the next build, so orphaned trigger
tables can't linger either. Event-payload schema drift (target job exists but
the stored payload no longer parses) is a genuine code-contract bug and is
deliberately left loud — it throws and is reported rather than self-healed.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Event→job bindings layered on @plugins/jobs. Plugins declare events with typed filter columns via defineTriggerEvent, subscribers bind jobs via trigger().
- Load-bearing: yes
- Server:
  - Uses: `database.db`, `infra/endpoints.HttpError`, `infra/endpoints.implement`, `infra/jobs.defineJob`, `infra/jobs.EnqueueTx`, `infra/jobs.getAllRegisteredJobNames`, `infra/jobs.UNSAFE_getRegisteredJob`, `infra/jobs.UNSAFE_installDurableHooks`
  - DB schema: `plugins/infra/plugins/events/server/internal/event.ts`, `plugins/infra/plugins/events/server/internal/tables.ts`
  - Exports: Types: `DefineTriggerEventSpec`, `EmitTx`, `EventHandle`, `EventSource`, `FilterSlot`, `TriggerSpec`, `UnsafeTriggerByNameSpec`; Values: `_event_emissions`, `defineTriggerEvent`, `deleteTrigger`, `deleteTriggersFor`, `EMISSIONS_CAP`, `eventEmissionsResource`, `eventTriggersResource`, `trigger`, `Trigger`, `triggerTableRegistry`, `UNSAFE_triggerByName`
  - Register: `defineJob('events.dispatch')`, `UNSAFE_installDurableHooks()`
  - Resources: `event-emissions` (invalidate), `event-triggers` (invalidate)
  - Routes: `GET /api/events/emissions`, `GET /api/events/triggers`, `DELETE /api/events/triggers/:id`, `PATCH /api/events/triggers/:id`
- Core:
  - Uses: `infra/endpoints.defineEndpoint`, `primitives/live-state.resourceDescriptor`
  - Exports: Types: `EmissionRow`, `EmissionsPayload`, `PatchTriggerBody`, `TriggerRow`, `TriggersPayload`; Values: `deleteTriggerEndpoint`, `EmissionRowSchema`, `EmissionsPayloadSchema`, `eventEmissionsResource`, `eventTriggersResource`, `listEmissions`, `listTriggers`, `patchTriggerBodySchema`, `patchTriggerEndpoint`, `TriggerRowSchema`, `TriggersPayloadSchema`
- Cross-plugin:
  - Imported by: `apps/pages/content-search`, `apps/pages/history`, `apps/workflows/engine`, `build`, `conversations`, `conversations/conversation-category`, `conversations/conversation-preprompt`, `conversations/conversation-progress`, `conversations/conversation-view/turn-summary`, `conversations/conversations-view/queue`, `improve`, `infra/events-test`, `infra/git-watcher`, `page/attachment-block`, `page/editor`, `page/inline-date`, `page/links`, `tasks`, `tasks/task-title`, `tasks/tasks-core`

<!-- AUTOGENERATED:END -->
