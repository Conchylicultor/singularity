// The registry-bound options the editor seeds and reads a block's Y.XmlText
// with, so a token plugin's round-trip test uses exactly the live option set.
export { blockTextRunsOptions } from "../internal/block-text-extensions";
