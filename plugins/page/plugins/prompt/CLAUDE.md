# prompt

Umbrella for the `/prompt` page block — the block **is** the prompt. Its text is
ordinary page content (editable, formattable, versioned, searchable like any
other block) and it carries a launch control that turns that text into an agent
run, without leaving the page.

Split into two sub-plugins so that the two unrelated UIs that consume the
task↔block link (the block renderer, and the task-detail origin section over in
the pages app) don't drag each other into their closures:

- **`link`** — the data layer: the `tasks_ext_prompt_block` entity extension,
  the two reads over it, the create-task endpoint, and the `pages` task category.
- **`block`** — the `prompt` block type itself: the `defineBlock` handle, the
  renderer (text editor + launch control + launched-conversation chips), and the
  server-side `data` schema registration.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Umbrella for the `/prompt` page block: the task↔block link data layer and the block type that launches agents from a page.
- Sub-plugins:
  - **`block`** — Prompt block type: block text plus a launch control that turns it into an agent run, and chips for the conversations it launched. Prompt block type: registers its `data` schema (plain block text) at the server write boundary.
  - **`link`** — Task↔prompt-block link: reads the tasks a prompt block launched (useBlockPromptTasks) and the page/block a task came from (usePromptTaskLink), and creates a provenance-stamped task (createPromptTask). No UI of its own. Owns the tasks_ext_prompt_block side-table: the page/block a task was launched from, the block-keyed and task-keyed live reads over it, the create-task endpoint, and the Pages task category.

<!-- AUTOGENERATED:END -->
