import { yieldFixtures } from "./internal/yield-fixtures";

// Default-export `LayoutFixture[]` — the shape codegen collects into
// `fixtures.generated.ts`. JSX lives in `internal/yield-fixtures.tsx`.
export default yieldFixtures;
