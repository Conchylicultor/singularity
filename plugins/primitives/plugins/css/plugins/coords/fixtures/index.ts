import { coordsFixtures } from "./internal/coords-fixtures";

// Default-export `LayoutFixture[]` — the shape codegen collects into
// `fixtures.generated.ts`. JSX lives in `internal/coords-fixtures.tsx`.
export default coordsFixtures;
