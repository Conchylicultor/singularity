import { pinFixtures } from "./internal/pin-fixtures";

// Default-export `LayoutFixture[]` — the shape codegen collects into
// `fixtures.generated.ts`. JSX lives in `internal/pin-fixtures.tsx`.
export default pinFixtures;
