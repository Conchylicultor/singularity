import { uiKitFixtures } from "./internal/ui-kit-fixtures";

// Default-export `HarnessFixture[]` — the shape codegen collects into
// `fixtures.generated.ts`. JSX lives in `internal/ui-kit-fixtures.tsx`.
export default uiKitFixtures;
