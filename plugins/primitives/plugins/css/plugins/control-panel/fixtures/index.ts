import { controlPanelFixtures } from "./internal/control-panel-fixtures";

// Default-export `HarnessFixture[]` — the shape codegen collects into
// `fixtures.generated.ts`. JSX lives in `internal/control-panel-fixtures.tsx`.
export default controlPanelFixtures;
