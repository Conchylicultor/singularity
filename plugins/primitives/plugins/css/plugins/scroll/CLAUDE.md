# scroll

The scroll-container box. ~100 call sites re-derived `min-h-0 flex-1
overflow-y-auto` by hand; `<Scroll>` owns that combination as one role.

## Why overflow and fill are one concern

A scroll pane inside a flex column needs **both** `flex-1` (claim the leftover
main-axis space) **and** `min-h-0` (allow shrinking below intrinsic content
height, so the overflow actually engages). Ship only `flex-1` and the pane grows
to fit its content, pushing past the parent — the whole page scrolls instead of
the pane. So `fill` emits the pair together (`min-w-0 flex-1` on the `x` axis);
splitting `min-h-0` back out re-opens that footgun.

The perpendicular axis is clamped to `hidden` for `axis="y"`/`"x"` so a vertical
scroller never sprouts a stray horizontal bar.

## Scope

Owns only overflow + the fill policy. **Sizing stays in `className`** — `h-*` /
`max-h-*` are not banned, so a fixed-height scroller is `<Scroll
className="max-h-96">` and a full-height one is `<Scroll className="h-full">`.

## Props

- **`axis`** — `"y"` (default) | `"x"` | `"both"`.
- **`fill`** — emit the flex-child fill pair. Default false.
- **`hideScrollbar`** — `no-scrollbar` (scrollable, no chrome). Default false.
- **`isolate`** — new stacking context (`isolate`). Default false.
- **`as`** — host element. Default `div`. **`className`** composes last.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Scroll-container layout primitive: <Scroll axis fill> owns overflow AND the flex-child fill policy (min-h-0 flex-1) as one role.
- Web:
  - Uses: `primitives/css/ui-kit.cn`
  - Exports: Types: `ScrollAxis`, `ScrollProps`; Values: `Scroll`, `scrollClasses`
- Cross-plugin:
  - Imported by: `apps-core/surface/floating/wallpaper`, `apps-core/tab-bar`, `apps/pages/page-tree`, `apps/pages/starred`, `apps/sonata/library`, `apps/sonata/notation`, `apps/sonata/songsheet`, `apps/sonata/track-mixer`, `apps/story/pages-integration`, `apps/story/renderers/slides`, `apps/studio/compositions`, `apps/studio/contributions`, `apps/studio/contributions/tables/sample-rows`, `apps/studio/release/release-logs`, `apps/workflows/definitions`, `build`, `build/build-logs`, `code-explorer/file-resolve`, `config_v2/settings`, `conversations/conversation-preprompt`, `conversations/conversation-view/code/docs-button`, `conversations/conversation-view/code/file-pane`, `conversations/conversation-view/dependencies`, `conversations/conversation-view/jsonl-viewer`, `conversations/conversation-view/jsonl-viewer/attachment/nested-memory`, `conversations/conversation-view/jsonl-viewer/code-listing`, `conversations/conversation-view/jsonl-viewer/tool-call`, `conversations/conversation-view/jsonl-viewer/tool-call/agent`, `conversations/conversation-view/jsonl-viewer/tool-call/bash`, `conversations/conversation-view/jsonl-viewer/tool-call/skill`, `conversations/conversation-view/jsonl-viewer/tool-call/task-tools`, `conversations/conversation-view/jsonl-viewer/tool-call/workflow`, `conversations/conversation-view/jsonl-viewer/tool-call/write`, `conversations/conversation-view/prompt-templates`, `conversations/conversations-view/data-view/history`, `conversations/conversations-view/data-view/queue`, `conversations/recover`, `debug/broadcasts`, `debug/claude-cli-calls`, `debug/live-state-churn/emit`, `debug/live-state-health`, `debug/logs`, `debug/memory`, `debug/queue`, `debug/read-set`, `debug/render-profiler`, `debug/reports`, `debug/slow-ops/cluster`, `debug/worktree-cleanup`, `debug/zero-test`, `fields/json/config`, `infra/events-test`, `page/page-link`, `plugin-meta/plugin-health`, `plugin-meta/plugin-view`, `plugin-meta/plugin-view/file-tree`, `primitives/css/column`, `primitives/css/layout-harness`, `primitives/data-view`, `primitives/diff-view`, `primitives/folder-picker`, `primitives/icon-picker`, `primitives/pane`, `primitives/syntax-highlight`, `review`, `review/code-review`, `review/config-defaults`, `shell/notifications`, `stats/cost`, `tasks/task-detail`

<!-- AUTOGENERATED:END -->
