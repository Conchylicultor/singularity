{
  "name": "@singularity/plugin-primitives-css-layout-harness",
  "description": "Layout-primitive geometry regression harness: a declarative fixtures catalog (one fixtures/index.ts per primitive) measured by a generic geometry oracle that asserts no-overlap / no-clip / truncation-onset invariants.",
  "private": true,
  "version": "0.0.1",
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
