import { frameFixtures } from "./internal/frame-fixtures";

// Default-export `LayoutFixture[]` — the shape codegen collects into
// `fixtures.generated.ts`. JSX lives in `internal/frame-fixtures.tsx`.
export default frameFixtures;
