import { truncatingTextFixtures } from "./internal/truncating-text-fixtures";

// Default-export `LayoutFixture[]` — the shape codegen collects into
// `fixtures.generated.ts`. JSX lives in `internal/truncating-text-fixtures.tsx`.
export default truncatingTextFixtures;
