import { textFixtures } from "./internal/text-fixtures";

// Default-export `LayoutFixture[]` — the shape codegen collects into
// `fixtures.generated.ts`. JSX lives in `internal/text-fixtures.tsx`.
export default textFixtures;
