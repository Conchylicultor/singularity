import { overlayFixtures } from "./internal/overlay-fixtures";

// Default-export `LayoutFixture[]` — the shape codegen collects into
// `fixtures.generated.ts`. JSX lives in `internal/overlay-fixtures.tsx`.
export default overlayFixtures;
