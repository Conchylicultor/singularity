export { TestSurface, createTestSurfaceStore } from "./surface-fixture";
// The module-level store standalone code falls back on; a suite that rebinds the
// live store puts this one back afterwards.
export { defaultStore } from "../pane";
