# element-size

The single sanctioned home for the "measure an element via `ResizeObserver`"
idiom, hand-rolled inconsistently across the repo before this primitive.

## API

- `useElementSize<T>(target?)` → `[callbackRef, { width, height }]`
  Reactive size via `getBoundingClientRect` (fractional, transform-aware).
  Attach the returned callback ref to the node; `{0,0}` until first measure.
  Pass a `target` getter to measure a different node than the one the ref is on
  (e.g. `(el) => el.offsetParent`).

- `useResizeObserver(target, onResize, { debounce?, deps? })` → `void`
  The substrate. `target` is a `RefObject`, an array of refs, or a getter
  (`() => Element | Element[] | null`). Runs `onResize` synchronously on mount
  (and when `deps` change), then on every resize — RAF-debounced unless
  `debounce: false`. `onResize` is stabilised internally, so the observer never
  re-subscribes on closure churn; use `deps` to force a re-measure.

## Why a synchronous initial measure

The first `onResize` runs synchronously inside the layout effect (not behind
RAF). This avoids a first-paint flash AND lets overflow/size decisions resolve
under a no-op `ResizeObserver`. Do not defer it.

That second property is what makes this primitive mountable in jsdom, which ships
no `ResizeObserver` and has no layout engine. The shared DOM-test setup
(`test/setup.ts`) installs one **inert** observer globally — not a polyfill, since
a 0x0 box that never resizes would give a polyfill nothing to report. Tests
therefore get the synchronous measure and nothing more: a test needing a real size
stubs the measurement source itself (e.g. `expandable`'s `offsetHeight` fixture),
and a test needing to drive resizes installs a drivable observer via
`vi.stubGlobal`.

## Don't hand-roll `new ResizeObserver`

Enforced by the `resize-observer-safety` lint rule
([`no-raw-resize-observer`](../../../framework/plugins/tooling/plugins/lint/plugins/resize-observer-safety/lint/no-raw-resize-observer.ts)).
Route size/resize observation through this primitive; if you have a genuinely
different need, extend the primitive rather than allowlisting a copy.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Element-size ResizeObserver idiom as a primitive: useElementSize(target?) reactively measures an element's size (callback ref, getBoundingClientRect, supports attach-one-node-measure-another via a target getter), and useResizeObserver(target, onResize, {debounce, deps}) is the substrate — synchronous initial measure, RAF-debounced resize callbacks, auto cleanup. The single sanctioned home for the hand-rolled ResizeObserver-for-size idiom.
- Web:
  - Uses: `primitives/latest-ref.useEventCallback`
  - Exports: Types: `ElementSize`, `ResizeTarget`; Values: `useElementSize`, `useResizeObserver`
- Cross-plugin:
  - Imported by: `apps-core/surface/floating`, `apps/prototypes/gallery`, `apps/sonata/notation`, `apps/sonata/piano-roll`, `primitives/collapsible-wrap`, `primitives/css/sticky/stack`, `primitives/data-table`, `primitives/data-view`, `primitives/data-view/gallery`, `primitives/expandable`, `primitives/graph-canvas`, `primitives/overflow-menu`, `primitives/pane`, `primitives/responsive-overflow`, `primitives/terminal`, `reorder`, `screenshot`

<!-- AUTOGENERATED:END -->
