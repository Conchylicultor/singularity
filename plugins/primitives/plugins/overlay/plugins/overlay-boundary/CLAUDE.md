# overlay-boundary

A React-only leaf primitive that gives **transient overlay content** its own
error boundary, so a crash inside an open overlay stays contained to that overlay
rather than bubbling up to the slot-level `PluginErrorBoundary` that wraps the
launching chrome (e.g. the action-bar trigger).

There are exactly **two** wrap sites, and neither is a per-surface one:
ui-kit's `OverlayPanel` — the one panel every floating surface (popover,
dropdown, select, dialog, caret surface) is composed from — and `TooltipContent`,
which is deliberately not an `OverlayPanel` (a one-line label with its own
chrome, where a max-height clamp is meaningless).

`OverlayBoundary` is a plain React `Component` class. When healthy it renders
`children` with no DOM node of its own (transparent, like `SingleLineProvider`).
When a child throws during render it renders a fallback. Isolation is a pure
consequence of React resolving to the *nearest* boundary: the inner
`OverlayBoundary` catches before the crash can reach the outer per-slot boundary
around the trigger.

## The fallback-injection seam (why the boundary lives here)

ui-kit's base-ui `*Content` components are the single shared choke point for
every overlay, so that is where the boundary must be wrapped. But ui-kit sits at
the bottom of the plugin DAG and `error-boundary` transitively depends on it
(`error-boundary → css/text → ui-kit`). ui-kit therefore **cannot** import
`error-boundary`'s rich `CrashFallback` — that would close the cycle
`ui-kit → error-boundary → css/text → ui-kit`, which the boundary checker
rejects (the same constraint ui-kit documents for `Frame`).

The resolution is a module-level fallback registry. This leaf depends on **only
`react`** (terminal edge — no cycle). ui-kit imports the leaf and wraps its
`*Content` children in `OverlayBoundary`. `error-boundary`, which sits *above*
ui-kit, calls `registerOverlayFallback(...)` at boot to inject the existing
`CrashFallback` (routing overlay crashes through `boundaryReportSink` → reports
and the `ErrorBoundary.Action` "Fix" button, unchanged). The boundary logic
lives low; the fallback UI is filled from above.

The built-in fallback (a text-only Retry `<button>`) exists only for the
pre-registration edge; in the real app the injected `CrashFallback` always wins.

## No per-surface tag — `children` is the whole API

Don't reintroduce a `kind` prop. It would feed only the fallback's `report.slot`
(the word in the chip's `… crashed` label), which the crash report does not need:
the fingerprint is `sha256(errorType + top stack frames)`
(`reports/crash/core/crash-kind.ts`), so a tag can never split or merge two
crashes, and the captured `componentStack` already names the real consumer chain.
The registration hardcodes `slot: "overlay"`.

## Reset semantics

base-ui Popups unmount their content on close, so closing and reopening a
crashed overlay remounts `OverlayBoundary` fresh and clears the error for free.
The fallback's **Retry** button covers residual cases (`keepMounted` surfaces).

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: React-only leaf error boundary for transient overlay content (popover/dialog/dropdown/select/tooltip/floating): OverlayBoundary catches a crash inside overlay content and renders a fallback injected via registerOverlayFallback, so the crash stays contained to the overlay instead of taking down the launching chrome. Sits below ui-kit so it can be wrapped around every *Content without closing the ui-kit → error-boundary cycle.
- Web:
  - Uses: `primitives/scope/install-sink.defineInstallSink`
  - Exports (types): `OverlayFallbackProps`
  - Exports (values):
    - `OverlayBoundary`
    - `registerOverlayFallback`
- Cross-plugin:
  - Imported by:
    - `primitives/css/ui-kit`
    - `primitives/error-boundary`

<!-- AUTOGENERATED:END -->
