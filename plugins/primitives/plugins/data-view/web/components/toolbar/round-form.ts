/**
 * The at-rest glyph colour of a `round`-form ghost trigger: the faint text tier.
 * The ghost variant's own `hover:` / `aria-expanded:` text-foreground lifts it
 * when pointed at or open. A ghost button has no resting colour of its own (it
 * inherits the text colour), which on a round capsule reads as a row of bright
 * circles competing with the search.
 */
export const ROUND_AT_REST = "text-faint-foreground";
