/**
 * Opaque per-view options for the table view, passed via
 * `DataViewProps.viewOptions["table"]`. Empty for now; reserved for future
 * per-column overrides so the pattern stays uniform across view children.
 */
export interface TableViewOptions {}
