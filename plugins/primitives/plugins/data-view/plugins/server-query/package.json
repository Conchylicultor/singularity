{
  "name": "@singularity/plugin-primitives-data-view-server-query",
  "description": "Server half of a server-delegated DataView over the one filter language: bind a source's declared filterable columns, strictly decode the wire filter, compile it with filterSql, plus the DataViewServer.QueryAugmentor registry. Composes the field-agnostic keyset seek + cursor codec from primitives/keyset.",
  "private": true,
  "version": "0.0.1"
}
