import { sectionCardFixtures } from "./internal/section-card-fixtures";

// Default-export `HarnessFixture[]` — the shape codegen collects into
// `fixtures.generated.ts`. JSX lives in `internal/section-card-fixtures.tsx`.
export default sectionCardFixtures;
