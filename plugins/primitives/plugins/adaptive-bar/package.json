{
  "name": "@singularity/plugin-primitives-adaptive-bar",
  "description": "Overflow as relocation: the pure fit math (rung assignment, width ledger, dock move plan) behind a bar that shrinks its widgets and relocates the rest without rendering any of them twice.",
  "private": true,
  "version": "0.0.1",
  "dependencies": {
    "@floating-ui/react-dom": "^2.1.8"
  }
}
