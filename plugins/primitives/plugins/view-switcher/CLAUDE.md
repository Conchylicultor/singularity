# view-switcher

The shared **view-switching primitive**. It offers two things:

- The `<ViewSwitcher>` **component** — borderless ghost-pill `SegmentedControl`
  chrome mapping `{ id, title, icon }` options to a single-select switcher. It
  stays **pure-presentational**: selection state is owned and passed by the
  caller (`activeId` + `onSelect`).
- The opt-in **`useActiveViewId(storageKey)` hook** — device-local active-id
  persistence, reading/writing `${storageKey}:active-view` in localStorage
  (DOMException-guarded). Returns `{ activeViewId, setActiveView }`. This is the
  *single canonical* active-id helper: both `tabbed-view` (bespoke hosts) and
  `data-view`'s `view-core` (named-instance engine) consume it from here, so the
  selection-persistence logic exists in exactly one place. The component does not
  depend on the hook — a caller can persist selection however it likes.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Presentational view-switcher chrome: borderless ghost-pill SegmentedControl mapping {id,title,icon} options to a single-select switcher (pure chrome — selection state stays with the caller), plus the opt-in device-local active-id helper useActiveViewId.
- Web:
  - Uses: `primitives/css/toggle-chip.SegmentedControl`
  - Exports: Types: `ActiveViewState`, `ViewSwitcherOption`, `ViewSwitcherProps`; Values: `useActiveViewId`, `ViewSwitcher`
- Cross-plugin:
  - Imported by: `primitives/data-view/view-core`, `primitives/tabbed-view`, `tasks/task-deps-tree`

<!-- AUTOGENERATED:END -->
