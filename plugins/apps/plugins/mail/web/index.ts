import type { PluginDefinition } from "@plugins/framework/plugins/web-sdk/core";

export default {
  collapsed: true,
  description: "Mail — a Gmail-class client.",
  contributions: [],
} satisfies PluginDefinition;
