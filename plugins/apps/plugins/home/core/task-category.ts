// The category tasks filed from the Home app's cards (building a new app) are
// stamped with. The server barrel contributes the matching TaskCategory
// registration.
export const APPS_CATEGORY_ID = "apps";
