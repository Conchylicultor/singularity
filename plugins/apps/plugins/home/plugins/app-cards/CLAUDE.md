# app-cards

Contributes the launcher grid to `Home.Section`: one card per registered app
(read generically via `Apps.App.useContributions()`, excluding the Home app
itself via `useCurrentAppId()`).

The grid is a `DataView<ActiveApp>` (the shared `primitives/data-view` surface),
so it gets search and view-state persistence for free. Each app is one `text`
field (`name`) plus an **icon cover** supplied through the gallery default-card
`cover` producer — no custom card component. The "New app" affordance is a
toolbar action (the DataView `actions` slot), not a trailing grid card; it is a
no-op for now. `viewOptions` is passed as a plain literal rather than importing
the gallery child, per data-view's collection-consumer separation.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Launcher grid of one card per installed app, plus the new-app placeholder.
- Web:
  - Contributes: `Home.Section` "Apps" → `AppGrid`
  - Uses: `apps.ActiveApp`, `apps.Apps`, `apps.useCurrentAppId`, `apps/home/shell.Home`, `primitives/data-view.DataView`, `primitives/icon-button.IconButton`

<!-- AUTOGENERATED:END -->
