# loop

Sonata A–B practice loop: a draggable loop region on the progression bar, a
toolbar Loop toggle, and `L`/`[`/`]` shortcuts that cycle playback within `[A, B]`.

The loop is also surfaced on the piano roll's falling-note timeline — a
scroll-synced `[A, B]` band with `A`/`B` boundary lines, contributed (view-only)
through the generic `Sonata.TransportOverlay` slot so it scrolls glued to the
notes. Dragging the bounds stays on the progression bar.

Because the loop is usually taller than the lookahead, a boundary often sits
off-screen. The loop also surfaces a sticky off-screen `A`/`B` edge indicator —
a small arrow chip pinned to the lane's top/bottom edge (screen-anchored via the
new `Sonata.TransportEdge` slot, mounted outside the scroll layer) pointing
toward a boundary that has scrolled past the lookahead, complementing the
scroll-synced band. The content-space band label and the edge chip are mutually
exclusive: a single edge-bucket threshold (which also reserves the HUD's vertical
zone) decides per boundary whether its in-band label or its edge chip shows.

The loop transport state (`loop`/`setLoop`) and the wrap live in the `shell`
plugin alongside `isPlaying`/`tempoScale`; this plugin owns the whole UI.

**Seamless wrap (no audio gap):** the wrap is no longer an event-driven restart.
The transport anchors once and the rAF tick *computes* the looped position with a
deterministic time-fold (`foldLoopTime` in `score/core`); the audio engine's
scheduler pre-schedules the same iterations end to end from the same anchor +
bounds (its cumulative-beat path), so the sound and the cursor wrap together with
no teardown and no silence at the `B`→`A` seam. The schedule is rebuilt only on a
deliberate change — toggling the loop, seeking, or moving the bounds — never on a
plain wrap; tempo changes follow the existing loop-aware `retime` path.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Sonata A–B practice loop: a draggable loop region on the progression bar, a toolbar Loop toggle, and L/[/] shortcuts that cycle playback within [A, B].
- Web:
  - Contributes:
    - `SonataProgress.Marker` "loop" → `LoopRegion`
    - `Sonata.TransportOverlay` "loop" → `LoopRollRegion`
    - `Sonata.TransportEdge` "loop" → `LoopRollEdge`
    - `SonataToolbar.End` "loop-toggle" → `LoopToggle`
    - `Sonata.Effect` "loop-shortcuts" → `LoopShortcuts`
  - Uses:
    - `apps/sonata/progress/scrubber.railBandClass`
    - `apps/sonata/progress/scrubber.SonataProgress`
    - `apps/sonata/shell.Sonata`
    - `apps/sonata/shell.SonataToolbar`
    - `apps/sonata/shell.useCursorApi`
    - `apps/sonata/shell.useCursorSelector`
    - `apps/sonata/shell.useLaneInsets`
    - `apps/sonata/shell.useSonata`
    - `primitives/css/inline.Inline`
    - `primitives/css/pin.Pin`
    - `primitives/css/spacing.Stack`
    - `primitives/css/ui-kit.cn`
    - `primitives/css/ui-kit.ControlSizeProvider`
    - `primitives/hover-reveal.hoverRevealClass`
    - `primitives/hover-reveal.useHoverReveal`
    - `primitives/icon-button.IconButton`
    - `primitives/shortcuts.useSurfaceShortcuts`

<!-- AUTOGENERATED:END -->
