// The category tasks filed from the Deploy app (a failed deploy's
// investigation) are stamped with. The server barrel contributes the matching
// TaskCategory registration.
export const DEPLOY_CATEGORY_ID = "deploy";
