# story

The story page — where equin came from.

A placeholder today: the page's heading ("How equin came to be" and the one
paragraph that sets it up), then the site's "more details soon" note. It renders
whatever is contributed to `WebsiteStory.Section`; write the story as a new
sub-plugin under here, not as an edit to this one — and remove the
`WebsiteSoon` note from the pane when it lands.

The homepage links here through
[`landing/story-link`](../../../landing/plugins/story-link/CLAUDE.md); keep the
two sets of words the same, so following the link confirms rather than
re-introduces.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: The story page of the equin website: the /website/story pane answering 'how did equin come to be?', its Story nav link, and the WebsiteStory.Section slot the story is written into.
- Web:
  - Slots: `WebsiteStory.Section`
  - Contributes:
    - `Pane.Register` "website-story"
    - `WebsiteHeader` "story" → `StoryNavItem`
  - Uses:
    - `apps/website/shell.WebsiteChrome`
    - `apps/website/shell.WebsiteHeader`
    - `apps/website/shell.WebsiteHero`
    - `apps/website/shell.WebsiteNavLink`
    - `apps/website/shell.WebsiteSoon`
    - `primitives/pane.defineRoute`
    - `primitives/pane.Pane`
    - `primitives/pane.useOpenPane`
    - `primitives/slot-render.defineRenderSlot`
  - Exports (values):
    - `storyPane`
    - `WebsiteStory`
- Cross-plugin:
  - Imported by: `apps/website/landing/story-link`

<!-- AUTOGENERATED:END -->
