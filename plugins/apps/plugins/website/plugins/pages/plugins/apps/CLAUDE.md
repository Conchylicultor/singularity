# apps

The applications page at `/website/apps` — the vision for applications.

A placeholder today: the page's heading ("A vision for applications" and why
now), then the site's "more details soon" note. It owns the
`WebsiteApps.Section` slot the page will be written into, so writing it means
adding a plugin, never editing this one — and removing the `WebsiteSoon` note
from the pane.

The nav link is contributed into the shell's shared `WebsiteHeader`, so the
shell never names this page. The homepage's second and third layer cards open
it.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: The applications page of the equin website: the /website/apps pane on the vision for applications (a placeholder heading for now), its Apps nav link, and the WebsiteApps.Section slot the page is written into.
- Web:
  - Slots: `WebsiteApps.Section`
  - Contributes:
    - `Pane.Register` "website-apps"
    - `WebsiteHeader` "apps" → `AppsNavItem`
  - Uses:
    - `apps/website/shell.WebsiteChrome`
    - `apps/website/shell.WebsiteHeader`
    - `apps/website/shell.WebsiteHero`
    - `apps/website/shell.WebsiteNavLink`
    - `apps/website/shell.WebsiteSoon`
    - `primitives/pane.defineRoute`
    - `primitives/pane.Pane`
    - `primitives/pane.useOpenPane`
    - `primitives/slot-render.defineRenderSlot`
  - Exports (values):
    - `appsPane`
    - `WebsiteApps`
- Cross-plugin:
  - Imported by: `apps/website/landing/layers`

<!-- AUTOGENERATED:END -->
