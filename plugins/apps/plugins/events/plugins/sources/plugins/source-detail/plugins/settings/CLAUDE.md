# settings

The Settings card of the source side-pane: the source's editable values — the
row's own `name`, then the type's `configFields` — rendered through `config_v2`'s
`FieldRenderer` dispatch as one form.

**This plugin names no source type, and adding one must not change this file.**
That is the whole payoff of putting per-type config in a `core/` `FieldsRecord`:
a new type ships a field record + an extractor and gets a working form.

- Autosave is per field, not per form. Each field renderer owns its own commit
  granularity (text commits on blur), so one PATCH lands per edit. The PATCH
  carries the FULL config because `UpdateEventSourceBody.config` replaces and
  revalidates the whole blob — a partial write can never leave a row whose
  config fails its type's schema. Two fields edited within one round trip would
  race (each sends its own snapshot); blur-granularity makes that practically
  unreachable, and the server revalidates either way.
- A stored blob that no longer fits its type's schema is *reported*
  (`readConfigValues` returns a result), never half-rendered — a partial form
  would silently drop the offending key on the next autosave.
- `name` is a COLUMN, not a config key: it renders above the type gates, so it
  stays editable when the type is uninstalled or its stored config no longer
  parses, and it survives a type with no `configFields` at all. It does not break
  the no-type-names rule — it belongs to the row, not to any type.
- No `useAvailable`: the card always carries the name, so it can never open onto
  emptiness. Don't reintroduce the gate — it would make a zero-config source
  (`manual`) unrenameable.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Settings section of the Events source side-pane: the source's own name plus the type's configFields, rendered generically through the fields FieldRenderer as one form with per-field autosave. Names no source type.
- Web:
  - Contributes: `EventSourceDetail.Section` "Settings" → `SourceSettingsSection`
  - Uses:
    - `apps/events/events-core.useUpdateEventSource`
    - `apps/events/sources.EventSourceDetail`
    - `apps/events/sources.readConfigValues`
    - `apps/events/sources.SourceConfigForm`
    - `apps/events/sources.useEventSource`
    - `apps/events/sources.useEventSourceType`
    - `config_v2/fields.FieldRenderer`
    - `infra/endpoints.getEndpointErrorMessage`
    - `primitives/css/control-panel.ControlPanelPane`
    - `primitives/css/placeholder.Placeholder`
    - `primitives/css/spacing.Stack`
    - `primitives/css/text.Text`
    - `primitives/loading.Loading`

<!-- AUTOGENERATED:END -->
