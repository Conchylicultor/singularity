# membership

Tints each explorer tree row by the inspected plugin's **membership state** in
the currently active composition (the working draft held by
`composition/web`'s active-composition store).

The feature is split across two slots:

- The **tint** is a `MembershipTint` contributed to `Explorer.TreeRowAccent`. It
  reads `useActiveMembership()` and returns just the wash element (a `size-full`
  tinted `div`) — the tree primitive's `RowChrome` owns the positioning, painting
  it into its own `pointer-events-none absolute inset-0` accent layer. The tint
  is translucent so the row text stays legible and the selected `bg-accent`
  blends through.
- The **pin buttons** are a `MembershipPin` contributed to `Explorer.TreeRowBadge`
  (the hover-revealed trailing actions cluster).

`MembershipTint`:

- **No active composition → renders nothing** (no tint by default).
- Otherwise it looks up `membership.get(node.id)` and tints by state:
  `entry` / `required` strong primary, `contributor` / `via-contributor` success,
  `available` faint info, `excluded` no band.

## Compare color mode (Increment 3)

When the store is in **compare mode** (`useDiffMap()` is non-null — both an active
and a compareWith composition are set), the tint uses **diff state** instead
of membership: `only-a` / `only-b` / `both` / `neither`. The diff palette is the
themeable **categorical** scale (`bg-categorical-4/7/2`), deliberately distinct
from the single-composition `primary` / `success` / `info` membership tints so the
two modes are never confusable. `neither` gets no band. Diff mode takes precedence
over membership when active.

`DIFF_TINT` (state → tint class) and `DIFF_LEGEND` (ordered legend rows) are
exported from this plugin's barrel so the Compositions pane's diff legend and
delta chips reuse the **exact same colors** the tree paints — single source of
truth, no drift.

On row hover it also surfaces a small **"Show closure from here"** pin button
calling `pinAsRoot(node.id)` — pin any plugin and the whole tree re-tints by its
closure.

The membership map is derived **once** per (active, graph) change by the store,
so the tint component only does a `Map.get`. Positioning is no longer this
plugin's concern: the tint is contributed to `Explorer.TreeRowAccent` and placed
by the `tree` primitive's `RowChrome` `accent` layer (a primitive-owned
`absolute inset-0` over the row), while the pin buttons are contributed to
`Explorer.TreeRowBadge` (the trailing actions cluster).

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Tints each explorer tree row by its membership state in the active composition, with a pin-as-root affordance.
- Web:
  - Contributes: `Explorer.TreeRowAccent` "membership" → `MembershipTint`, `Explorer.TreeRowBadge` "membership" → `MembershipPin`
  - Uses: `apps/studio/explorer.Explorer`, `apps/studio/graph.graphCanvasPane`, `apps/studio/membership-tint.STATE_TINT`, `plugin-meta/composition.DiffState`, `plugin-meta/composition.pinAsRoot`, `plugin-meta/composition.useActiveMembership`, `plugin-meta/composition.useDiffMap`, `plugin-meta/composition.useEnsureCompositionData`, `primitives/css/cluster.Cluster`, `primitives/css/ui-kit.cn`, `primitives/icon-button.IconButton`, `primitives/pane.openPane`
  - Exports: Values: `DIFF_LEGEND`, `DIFF_TINT`
- Cross-plugin:
  - Imported by: `apps/studio/compositions`

<!-- AUTOGENERATED:END -->
