// A throwaway git repo a check's input-keyed read-set is recorded and
// revalidated against, the way the check runner drives it.
export { createReadSetRepo, type ReadSetRepo } from "./read-set-repo";
