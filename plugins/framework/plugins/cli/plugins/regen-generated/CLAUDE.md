# regen-generated

`./singularity regen-generated` — re-derives every non-migration generated file
in the repo tree: barrel stubs, the plugin registries, the plugin docs
(`plugins-compact.md`, `plugins-details.md`, per-plugin `CLAUDE.md` autogen
blocks), the reorderable-slots / data-views / token-group-vars manifests, and
config origins.

It owns no pipeline of its own. It calls the same `regenerateRegistryCodegen` +
`regenerateManifestCodegen` that `./singularity build` calls, in the same order,
so a build immediately afterwards is a no-op — there is one repo-tree codegen
pipeline, and two commands that reach it.

`push` is the real caller: after a rebase, a merge driver may have auto-resolved
a generated file to a side that no longer matches the merged sources, and
regenerating is the repair. Paying for a whole build there would mean a build
lock, a Postgres wait and a DB fork for a pass that only rewrites tree files.

The one thing it adds is a refusal. Its output is about to be
`git add -A && git commit --amend`-ed by push, so before returning it asserts
that no config override still carries an `// @review` marker. Such an override
is a machine-seeded default nobody has looked at; amending it in would land
exactly what the marker exists to prevent. That assertion lives here rather than
in a check because it is bound to *this* command's position in push's sequence,
not to the state of the tree in general.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: `./singularity regen-generated` — the repo-tree half of build's codegen standalone (registries, barrels, plugin docs, manifests, config origins), for the post-rebase normalize step in `push`.

<!-- AUTOGENERATED:END -->
