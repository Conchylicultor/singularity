/** Log channel the final ranked report is dumped to on stop (headless reads). */
export const RENDER_PROFILER_CHANNEL = "render-profiler";

/** Global key for the window-level imperative profiler API. */
export const RENDER_PROFILER_GLOBAL = "__reactRenderProfiler";
