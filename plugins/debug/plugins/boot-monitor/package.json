{
  "name": "@singularity/plugin-debug-boot-monitor",
  "description": "Whole-boot monitor: a cheap per-worktree scheduled job that, once boot fully completes, mints ONE slow-op row (operationKind 'boot') + coherent trace per over-budget server boot, folding in the gateway-observed readiness wait when the gateway POSTed its report.",
  "private": true,
  "version": "0.0.1"
}
