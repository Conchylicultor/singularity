# zero-test

> **⚠️ Harness for the FROZEN Zero pilot — no further investment.** This pane is
> the pilot's only consumer; it is frozen along with it (not deleted — it stays
> as the verification harness for a possible future un-freeze). See the freeze
> decision and re-evaluation trigger in
> [`../../../database/plugins/zero/CLAUDE.md`](../../../database/plugins/zero/CLAUDE.md).

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Temporary verification harness: a Debug → Zero Test pane that renders the pilot tasks slice live through the Zero client adapter. Deleted once a real migration begins.
- Web:
  - Slots: `zeroTestPane.Actions` ← `primitives.pane`
  - Contributes:
    - `Pane.Register` "zero-test"
    - `DebugApp.Sidebar` "Zero Test" → `component`
  - Uses:
    - `apps/debug/shell.DebugApp`
    - `database/zero/client.useZeroResource`
    - `database/zero/client.ZeroRoot`
    - `primitives/app-shell.sidebarNavItem`
    - `primitives/css/row.Row`
    - `primitives/css/scroll.Scroll`
    - `primitives/css/spacing.Stack`
    - `primitives/css/text.Text`
    - `primitives/live-state.ResourceView`
    - `primitives/loading.Loading`
    - `primitives/pane.openPane`
    - `primitives/pane.Pane`
    - `primitives/pane.PaneChrome`
  - Exports (values): `zeroTestPane`
- Core:
  - Exports (types): `Schema`
  - Exports (values):
    - `permissions`
    - `schema`
    - `zql`

<!-- AUTOGENERATED:END -->
