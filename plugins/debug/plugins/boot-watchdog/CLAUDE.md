# boot-watchdog

Makes a **backend that never becomes ready loud immediately** — the report/bell
half of the wedged-boot signal whose timeline half (an open-ended boot bar)
already exists.

During the 2026-07-17 incident an **11.5-minute never-ready boot of main** sat in
`boot.jsonl`, consumed by nothing: the timeline rendered it as an open bar, but it
reached no report and no bell, so the investigating agent never saw it. This
plugin closes that gap. It is modeled byte-for-byte on `debug/boot-budget` (config
+ `ReportKind` + web `KindView`), differing only where the signal genuinely
differs.

## Why main-only (no `perWorktree`)

The monitor is **structurally** main-only — not a policy choice. A `perWorktree`
scheduled job dequeues only after its own backend has booted successfully (the
job queue runs on the ready backend), so a backend can never observe its OWN
wedged boot. Main reads every worktree's `boot` channel off the shared filesystem
(`readBootEvents` — exactly how the timeline reads them) even while the owning
backend is wedged, so it is the only place this observation can be made. The
`worktree` column on the filed row is therefore always `main`, which is why the
subject lives in the **fingerprint** (`boot-wedge:<worktree>`) and the payload,
never the row's own worktree.

## Superseded vs open

Each tick sweeps every worktree's boot channel (`readBootEvents(wt, lookbackMs)`)
for boots with no `ready` line whose un-ready duration has crossed
`bootReadyBudgetMs` (default 120 s):

- **superseded** — a later boot attempt already replaced it (`supersededAtMs`
  set). A closed, past outage. Filed **once** (a module-level
  `Set<wt:processStartedAt>`, boot-budget's dedup pattern) so it is on record
  post-hoc without re-filing the same dead outage every minute.
- **open** — the latest attempt, no successor. Could be wedged NOW or torn down.
  The gateway fleet list (`GET /gateway/worktrees`, 2 s timeout) is the
  discriminator: **any presence** — regardless of the gateway's `state` string —
  means the worktree still exists and its backend is being run, so a missing
  ready line is a live wedge. Gateway-`running` is a LOWER bar than our
  app-`ready` line (process up + socket open, but `onReady` may never have
  fired), so we must NOT gate on `state === "running"` (that would miss the exact
  wedged-now boots this exists to catch). Filed **each tick** (no dedup Set), so
  the row's `count` ≈ minutes wedged and the bell re-arms per the 15-min
  cooldown. If the gateway is unreadable this tick (`readFleet` → null), open
  evaluation is skipped rather than guessed.

## Load-bearing literals

The config `name` `boot-watchdog`, the job name `debug.boot-watchdog-monitor`, the
report kind `boot-wedge`, and the source `server-boot-watchdog` are explicit
literals — persisted config, report dedup, and the `SERVER_REPORT_SOURCES` union
depend on them; do not rename. The `durable-signals-accounted` check enforces that
the `boot` channel stays classified as filing this report.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Boot-wedge report renderer: a one-line Debug → Reports summary for the boot-wedge kind, plus the boot-watchdog budget/lookback config registration. Boot-watchdog monitor: a main-only per-minute scheduled job that sweeps every worktree's boot channel off the shared filesystem and files a deduped boot-wedge report for any backend that never reached its `ready` line within the boot budget — superseded (post-hoc, once) or open (gateway-confirmed wedged-now, re-filed each tick). Structurally main-only: a perWorktree job cannot observe its own wedged boot.
- Web:
  - Contributes: `ConfigV2.WebRegister`, `Reports.KindView` → `BootWedgeSummary`
  - Uses: `config_v2.ConfigV2`, `primitives/css/badge.Badge`, `primitives/css/inline.Inline`, `reports.Reports`
- Server:
  - Contributes: `ConfigV2.Register` "boot-watchdog", `report-kind` "boot-wedge"
  - Uses: `config_v2.ConfigV2`, `config_v2.getConfig`, `debug/boot-events.readBootEvents`, `infra/jobs.defineJob`, `infra/paths.listWorktreeDirs`, `primitives/log-channels.logsDirFor`, `reports.recordReport`, `reports.ReportKind`
  - Register: `defineJob('debug.boot-watchdog-monitor')`
- Core:
  - Uses: `config_v2.defineConfig`, `fields/bool/config.boolField`, `fields/int/config.intField`
  - Exports: Types: `BootWedgePayload`; Values: `bootWatchdogConfig`, `BootWedgePayloadSchema`

<!-- AUTOGENERATED:END -->
