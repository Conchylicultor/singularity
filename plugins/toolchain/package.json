{
  "name": "@singularity/plugin-toolchain",
  "version": "0.0.1",
  "private": true,
  "description": "Keeps the mise toolchain (bun, go, tmux, rust) on its latest releases: the declared tools with their floors and holds, the toolchain:resolved check, the `toolchain upgrade` command that gates a new version behind a before/after check+test comparison, and the daily job that files the auto-started task running it."
}
