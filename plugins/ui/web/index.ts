import type { PluginDefinition } from "@core";

export default {
  id: "ui",
  name: "UI",
  description:
    "Umbrella for pluggable UI components with switchable visual variants.",
  contributions: [],
} satisfies PluginDefinition;
