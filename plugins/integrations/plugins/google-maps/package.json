{
  "name": "@singularity/plugin-integrations-google-maps",
  "description": "Google Maps Platform access broker: the API key on the server, the reactive readiness state and the setup affordance on the web, so consumers never import @plugins/auth directly.",
  "private": true,
  "version": "0.0.1"
}
