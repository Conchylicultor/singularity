# run-outcome

The shared status axis every run kind maps into, and its one display.

`running / succeeded / partial / failed / canceled` — deliberately small, because
it is the axis a person filters and groups by *across* kinds, so it may only hold
distinctions true of every kind. `partial` exists because backup is the one kind
that can half-succeed, and flattening that into either neighbour loses the only
fact that matters. `canceled` covers superseded / interrupted / killed: nothing
went wrong, the run stopped being the answer.

An arm's finer native status is **not** lost — it stays an arm field, so
precision lives beside the shared axis instead of replacing it.

Own plugin, not `runs/core`, so `runs/web` can import the chip without
`run-outcome/web → runs/core → runs/web` closing a cycle.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: The shared run-outcome display: the colour/label metadata, the derived filter options, and the dot / chip / badge every run kind renders its outcome through — so a build row and a backup row cannot disagree about what `failed` looks like.
- Web:
  - Uses:
    - `primitives/css/badge.Badge`
    - `primitives/css/badge.BadgeVariant`
    - `primitives/css/inline.Inline`
    - `primitives/css/status-dot.StatusDot`
  - Exports (values):
    - `RUN_OUTCOME_META`
    - `RUN_OUTCOME_OPTIONS`
    - `RunOutcomeBadge`
    - `RunOutcomeChip`
    - `RunOutcomeDot`
- Cross-plugin:
  - Imported by: `runs`
- Core:
  - Exports (types): `RunOutcome`
  - Exports (values):
    - `isRunInFlight`
    - `RUN_OUTCOMES`
    - `RunOutcomeSchema`

<!-- AUTOGENERATED:END -->
