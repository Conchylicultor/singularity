# tab-surface

The per-tab render core. `TabSurface` mounts one tab's `PaneSurfaceProvider`,
reports its leaf title to the tabs store, and renders the app — identically for
every placement, so docked / floating-window / solo differ only in the box they
hand it.

## The surface box is the overlay host

The tab's outer box is a `<SurfaceOverlayHost>` (`primitives/surface-overlay`),
not a plain `relative size-full` div. That box is the only element that IS the
surface, so it is where a pane's full-surface overlay has to land: a pane can
fill its own tab — tab bar and app rail still on screen and clickable — instead
of reaching for `<ViewportOverlay>`, which covers them. Because the host sits
inside whatever box the placement gave the tab, that works under all three
placements with nothing placement-specific written anywhere.

It stays `relative size-full` for its original reason too: the sync-status
indicator's `Pin` anchors to this surface's corner, and the app render fills it.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Per-tab surface render core: TabSurface mounts a tab's PaneSurfaceProvider and reports its leaf title; AppTabsBody is the keep-alive fallback body that stacks every open tab.
- Web:
  - Uses:
    - `apps-core.Apps`
    - `apps-core/tabs.appPathFor`
    - `apps-core/tabs.loadScopePrefixFor`
    - `apps-core/tabs.Tab`
    - `apps-core/tabs.useTabs`
    - `primitives/css/theme-boundary.Theme`
    - `primitives/css/ui-kit.appThemeScope`
    - `primitives/pane.PaneBasePathContext`
    - `primitives/pane.PaneSurfaceProvider`
    - `primitives/pane.useIndexMatch`
    - `primitives/pane.usePaneTitle`
    - `primitives/pane.useRoute`
    - `primitives/pane.useRouteState`
    - `primitives/slot-render.renderIsolated`
    - `primitives/surface-overlay.SurfaceOverlayHost`
    - `primitives/sync-status.SyncStatusIndicator`
    - `primitives/sync-status.SyncStatusProvider`
    - `primitives/undo-redo.UndoRedoProvider`
    - `primitives/undo-redo.useUndoRedoShortcuts`
  - Exports (values):
    - `AppTabsBody`
    - `TabSurface`
- Cross-plugin:
  - Imported by:
    - `apps-core/layout`
    - `apps-core/surface`

<!-- AUTOGENERATED:END -->
