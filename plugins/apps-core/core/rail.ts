/**
 * The app rail's width — one value for the rail itself (the `rail` framing
 * variant sets it as `--app-rail-width`) and for the tab bar's leading mark,
 * which sits in the column above the rail so the first tab lines up with the
 * app's left edge.
 */
export const APP_RAIL_WIDTH = "2.5rem";
