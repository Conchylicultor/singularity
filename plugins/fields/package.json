{
  "name": "@singularity/plugin-fields",
  "version": "0.0.1",
  "private": true,
  "description": "Global unified fields primitive: the type-dimension registry (FieldType token + FieldIdentity + extends-chain resolution) shared across data surfaces.",
  "singularity": {
    "collapsed": true
  }
}
