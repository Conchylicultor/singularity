// The real bool operator set, for another plugin's test to lower authored
// filters through exactly what the Filter control runs (e.g. mail's authored
// mailbox tabs).
export { boolOperatorSet } from "../operator-set";
