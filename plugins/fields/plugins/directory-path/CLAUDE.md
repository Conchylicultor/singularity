# directory-path

The **directory-path** field type — an absolute host filesystem directory.

This barrel registers ONLY the `FieldIdentity` (label "Folder", `MdFolder`
icon, string `coerce`) in the `fields.identity` registry. The value type is a
plain `string`; what makes it more than `text` is its config renderer, a folder
picker.

- `plugins/config` — the `dirPathField` factory plus the renderer contributed
  to `config-v2.fields.renderer`. The renderer wraps the `folder-picker`
  primitive (`@plugins/primitives/plugins/folder-picker/web`): a typeable path
  input with a live validity indicator and a "Browse" popover that walks the
  host filesystem.

## Plugin reference

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Directory-path field type: identity only. The config-render capability (a folder picker) and the dirPathField factory live in the plugins/config sub-plugin.
- Web:
  - Contributes: `Fields.Identity` "directory-path"
  - Uses: `fields.Fields`
- Core:
  - Uses:
    - `fields.defineFieldIdentity`
    - `fields.defineFieldType`
  - Exports (values):
    - `directoryPathFieldType`
    - `directoryPathIdentity`
- Cross-plugin:
  - Imported by: `fields/directory-path/config`
- Sub-plugins:
  - **`config`** — Directory-path field type: config-render capability (folder picker for config-v2.fields.renderer) plus the dirPathField factory.

<!-- AUTOGENERATED:END -->
