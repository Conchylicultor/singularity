# string-list

The **string-list** field type of the unified fields primitive — an ordered
collection of plain strings (`string[]`).

This barrel registers ONLY the `FieldIdentity` (label "String List",
`MdFormatListBulleted` icon) in the `fields.identity` registry. Unlike `list`,
the value carries no per-item `id`/`rank` — it is a bare `string[]` that passes
through config_v2's collection-id injection untouched (`injectCollectionIds` in
`config_v2/server/internal/registry.ts` only processes fields that carry
`itemFields`). Use it when you need an ordered list of plain strings whose exact
contents and order matter (e.g. reorder directives `order` / `hidden`).

The config-render capability — the `stringListField` factory plus the textarea
renderer (one item per line) contributed to the frozen `config-v2.fields.renderer`
slot — lives in the `plugins/config` sub-plugin.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: String-list field type: identity only. The config-render capability and the stringListField factory live in the plugins/config sub-plugin.
- Web:
  - Contributes: `Fields.Identity` "string-list"
  - Uses: `fields.Fields`
- Core:
  - Uses: `fields.defineFieldIdentity`, `fields.defineFieldType`
  - Exports: Values: `stringListFieldType`, `stringListIdentity`
- Sub-plugins:
  - **`config`** — String-list field type: config-render capability (one-item-per-line textarea for config-v2.fields.renderer) plus the stringListField factory.

<!-- AUTOGENERATED:END -->
