# server-capabilities-loader

The eager-load trigger for the server-owned field capabilities. Its barrel imports
a generated side-effect manifest (`internal/eager.generated.ts`) of every
`fields/plugins/<type>/plugins/storage/server` barrel, so each
`Fields.Storage(...)` self-registers into the
`server-capabilities` eager index at module-eval — before any `defineEntity` body
runs — with NO filesystem scan.

This lives in its own plugin (not in the token-owning `server-capabilities`
library) precisely so that library stays a graph **sink**: importing the capability
barrels there would recreate the `fields ⇄ fields/<type>/storage`
import cycle (the barrels import the tokens back). Closure here is
`loader → barrels → server-capabilities` — a clean DAG.

Every eval-time consumer of `resolveFieldStorage` imports this barrel for
side-effect (`infra/entities`), so the eager index is populated before its first
resolve call. The manifest is
also the composition-closure edge that pulls the capability barrels into every
release bundle that ships those consumers.

The manifest is generated + committed; the `fields-eager-in-sync` check fails on
drift. Fix by running `./singularity build`.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Eagerly evaluates every fields storage capability barrel via a generated side-effect manifest, so each self-registers into the server-capabilities eager index. Eval-time consumers import this for side-effect.
- Server:
  - Uses:
    - `fields/bool/storage`
    - `fields/date/storage`
    - `fields/float/storage`
    - `fields/int/storage`
    - `fields/json/storage`
    - `fields/rank/storage`
    - `fields/tags/storage`
    - `fields/text/storage`
    - `fields/uuid/storage`
- Cross-plugin:
  - Imported by: `infra/entities`

<!-- AUTOGENERATED:END -->
