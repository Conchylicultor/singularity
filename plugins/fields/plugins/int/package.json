{
  "name": "@singularity/plugin-fields-int",
  "version": "0.0.1",
  "private": true,
  "description": "Integer field type: identity only, extends number — reuses number's cell and filter via the extends chain.",
  "singularity": {
    "collapsed": true
  }
}
