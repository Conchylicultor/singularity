{
  "name": "@singularity/plugin-fields-json-storage",
  "version": "0.0.1",
  "private": true,
  "description": "JSON field type: DB storage capability — a Postgres jsonb column, decoded by the field's own schema so a jsonField<T>'s shape is derived rather than asserted.",
  "singularity": {
    "collapsed": true
  }
}
