// The real tags operator set, for another plugin's test to lower authored
// filters through exactly what the Filter control runs (e.g. mail's authored
// mailbox tabs).
export { tagsOperatorSet } from "../operator-set";
