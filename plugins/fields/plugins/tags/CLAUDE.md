# tags

The **tags** (multi-value) field type of the unified fields primitive — a *set*
of string labels per row, rather than a single scalar.

This barrel registers ONLY the `FieldIdentity` (label "Tags", `MdLabel` icon; NO
`extends`, NO `coerce`) in the `fields.identity` registry. Unlike a scalar type,
a `tags` field carries its values through `FieldDef.values: (row) => string[]`
(not `value`), so the data-view search/filter paths treat it as an array.

The type's capabilities live in dedicated sub-plugins under `plugins/`:

- `plugins/config` — the `tagsField` factory + the multi-select chip renderer
  contributed to `config-v2.fields.renderer`. Its value is an open `string[]`
  rather than `z.enum(options)[]`; the sub-plugin's own CLAUDE.md says why.
- `plugins/filter` — the multi-select `FilterContribution` (`data-view.filter`):
  one `ToggleChip` per `field.options`, toggling membership of a
  `selected: string[]`, plus an **array-aware** predicate that keeps a row when
  its tag set intersects the selected set (match-any).

It deliberately does **not** `extends enum`: `extends` would resolve enum's
*scalar* predicate, which tests one value's membership in the selected set — the
wrong semantics for a multi-valued row. `tags` stands alone with its own
array-aware filter.

## The option contract

`FieldDef.options` is `{ value, label, variant?, hint? }[]` (the shared
`FieldOption`, `primitives/data-view/core`). **An option describes how it
presents** — the read chip takes its tint from `variant` (default `"muted"`) and
its tooltip from `hint`, so a surface no longer keeps a value→label map beside a
parallel value→variant map that every render site has to join by hand.

Deliberate no-ops, so nobody "finishes the job": the inline editor and the filter
operand input draw `ToggleChip`s, whose vocabulary is not `BadgeVariant`, and
stay untinted; group-by section labels and the control summaries are text-only;
server-side filter SQL never reads `options` at all, so a tint never reaches the
server.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Tags (multi-value) field type: identity only. The data-view filter (multi-select tag chips with array-aware match-any) lives in the plugins/filter sub-plugin.
- Web:
  - Contributes: `Fields.Identity` "tags"
  - Uses: `fields.Fields`
- Core:
  - Uses:
    - `fields.defineFieldIdentity`
    - `fields.defineFieldType`
  - Exports (values):
    - `tagsFieldType`
    - `tagsIdentity`
- Cross-plugin:
  - Imported by: `fields/tags/config`
- Sub-plugins:
  - **`config`** — Tags field type: config-render capability. Contributes the multi-select chip renderer to the config-v2.fields.renderer slot.
  - **`filter`** — Tags (multi-value) field type: data-view filter operator set (contains / contains-any-of …).
  - **`filter-sql`** — Tags field type: server filter-sql capability — operator→SQL fragments mirroring the data-view tags filter predicates.
  - **`inline`** — Tags (multi-value) field type: data-view inline cell editor (multi-select chip popover).
  - **`storage`** — Tags field type: DB storage capability — a Postgres jsonb column, decoded by the field's own schema so its string[] is derived rather than asserted.
  - **`table`** — Tags (multi-value) field type: data-view table cell (read-only tag chips).

<!-- AUTOGENERATED:END -->
