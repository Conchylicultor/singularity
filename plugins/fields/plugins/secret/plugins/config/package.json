{
  "name": "@singularity/plugin-fields-secret-config",
  "version": "0.0.1",
  "private": true,
  "description": "Secret field type: config-render capability (password input), encrypted storage provider, meta resource, and central reader, plus the secretField factory.",
  "singularity": {
    "collapsed": true
  }
}
