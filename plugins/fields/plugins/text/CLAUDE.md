# text

The **text** field type — the generic base text type of the unified fields
primitive.

This barrel registers ONLY the `FieldIdentity` (label, icon, string `coerce`)
in the `fields.identity` registry. The type's data-view capabilities live in
dedicated sub-plugins under `plugins/`:

- `plugins/table` — the read-only `TextCell` (`data-view.cell`).
- `plugins/filter` — the substring `FilterContribution` (`data-view.filter`).

Derived text types (e.g. `multiline-text`) declare `extends: textFieldType` and
reuse these capabilities via the `extends`-chain fallback.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Text field type: identity only. The data-view cell and filter (substring) capabilities live in the plugins/{table,filter} sub-plugins.
- Web:
  - Contributes: `Fields.Identity` "text"
  - Uses: `fields.Fields`
- Core:
  - Uses: `fields.defineFieldIdentity`, `fields.defineFieldType`
  - Exports: Values: `textFieldType`, `textIdentity`
- Cross-plugin:
  - Imported by: `fields/multiline-text`, `fields/text/config`
- Sub-plugins:
  - **`config`** — Text field type: config-render capability (single-line input for config-v2.fields.renderer) plus the textField factory.
  - **`filter`** — Text field type: data-view filter (substring contains control).
  - **`table`** — Text field type: data-view table cell (read-only text cell).

<!-- AUTOGENERATED:END -->
