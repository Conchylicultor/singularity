# text / storage

The **text** type's storage capability, and the first of the repo's three
**decoding** storage arms (`json` and `tags` are the others): it contributes
`Fields.Storage({ type, decode })` rather than `{ build }`, so it is handed the
FIELD's own schema and builds the column from it. Resolved by exact token through
`resolveFieldStorage("text")`.

That is what makes `enumTextField(...)` true rather than asserted. The field-record
analogue of `text("x").$type<Union>()` used to be exactly as empty as the
original: `$type` runs nothing, so the union was a claim about rows nobody
checked. Now the schema that declares the union is the schema that decodes the
column — on every read and every write (`parsedText`,
`plugins/database/plugins/sql-column`).

## The one discrimination

```ts
valueSchema instanceof ZodString ? text(name) : parsedText(name, valueSchema)
```

A plain `textField()` (or one refined with `.min(1)` — still a `ZodString`) keeps
a bare `text` column and pays nothing. Anything else gets a real decoder.

The branch is on the schema the author actually wrote, not a heuristic about
intent — and it is not an optimisation detail: decoding a column that is NOT
narrowed costs the **same** as decoding one that is (345 ns vs 322 ns per value)
for zero guarantee, so decoding every text column would spend roughly 10× more
than decoding only the narrowed ones. `internal/storage.test.ts` pins both
branches.

`getSQLType()` is `"text"` either way, so adopting the decoder generates no
migration (see `sql-column/CLAUDE.md`).

## The decoder runs on writes and on bound params too

One schema, both directions. Besides INSERT `.values()` and UPDATE `.set()`, the
encoder runs on every **bound comparison param** — `eq`, `inArray`, and the rest
route through `bindIfParam` → `Param(value, column)`.

A DataView filter operand is deliberately **not** one of them any more. It briefly
was, and that was wrong in both directions: a strict column threw on a saved view
holding a retired option, and a tolerant one *normalized* the operand and answered
a question nobody asked. A comparison operand is not a stored value, so
`server-query` now hands each filter builder the column as a plain expression
rather than the column itself, and the encoder never sees it. See
`primitives/data-view/server-query/CLAUDE.md`.

Design: `research/2026-08-25-global-decoded-entity-columns.md`, and
`research/2026-08-27-global-filter-operand-domain.md` for the filter boundary.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: Text field type: DB storage capability — a Postgres text column, decoded by the field's own schema so a narrowed text column (enumTextField) is derived rather than asserted.
- Server:
  - Contributes: `fields.storage` "text"
  - Uses:
    - `database/sql-column.parsedText`
    - `fields/server-capabilities.Fields`
- Cross-plugin:
  - Imported by: `fields/server-capabilities-loader`

<!-- AUTOGENERATED:END -->
