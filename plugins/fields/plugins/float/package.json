{
  "name": "@singularity/plugin-fields-float",
  "version": "0.0.1",
  "private": true,
  "description": "Float field type: identity only, extends number — reuses number's cell and filter via the extends chain.",
  "singularity": {
    "collapsed": true
  }
}
