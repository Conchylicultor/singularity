// MIGRATION: table moved to tasks-core — this stub keeps existing internal
// imports compiling. Remove in Phase 3 once all consumers use tasks-core.
export { _conversations } from "@plugins/tasks-core/server/internal/tables";
