# inflight

In-flight request deduplicator: `createInflight()` returns `{ run(key, fn), size }`.
Concurrent callers sharing a `key` share one execution of `fn` and one result;
the key clears the instant the promise settles, so it collapses *overlapping*
work without caching a settled value. A `Map<key, pending promise>` and nothing
more — releases the key in a `finally` so a rejection is shared by the in-flight
callers and then cleared (the next call retries fresh).

This is a *concurrency* deduplicator, not a cache — pair it with a TTL cache if
you also want to reuse a settled result. Never use it where each caller needs a
distinct side effect (mutations): the shared body runs once.

Used to collapse identical concurrent reads that would otherwise each spawn
CPU-heavy work:
- the endpoints primitive's `dedupe` option (`plugins/infra/plugins/endpoints`)
  collapses concurrent identical GETs onto one handler invocation;
- `getEditedFiles(worktreePath)` so the edited-files loader and the
  plugin-changes endpoint share one git-status batch per worktree.

See `research/2026-06-15-global-live-state-cascade-contention.md` (Change 5).

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Cross-plugin:
  - Imported by:
    - `framework/resource-runtime`
    - `infra/endpoints`
- Core:
  - Exports (types): `Inflight`
  - Exports (values): `createInflight`

<!-- AUTOGENERATED:END -->
