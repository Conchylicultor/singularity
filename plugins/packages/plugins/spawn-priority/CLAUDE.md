# spawn-priority

OS scheduling-priority demotion for background subprocess spawns. The host runs
one interactive main backend plus a fleet of background work (agent sessions,
DB forks, worktree checkouts, agent builds) that would otherwise compete at
equal CPU/IO priority and starve interactive latency — concurrency gates bound
*how many* heavy ops run, but a single build legitimately fans across every
core, so only OS priority can make one launch invisible.

- `backgroundArgv(argv)` — prefix an argv array for `Bun.spawn`.
- `backgroundPrefix()` — the same prefix as a shell-command string, for command
  strings executed by a shell we don't spawn (e.g. tmux session commands, which
  the shared tmux server forks — wrapping the tmux *client* would be a no-op).

Mechanism: darwinbg (`/usr/sbin/taskpolicy -b`), inherited by all children.
On Apple Silicon this pins the subtree to the efficiency cores and applies the
background disk-IO throttle tier (which only bites under IO contention).
Measured: `-c utility` gives ZERO CPU protection and must not be used; `-b`
keeps the P-cores free for the interactive backends. Details + experiment data:
`research/2026-07-07-global-background-work-priority-isolation.md`.

Escape hatch / A-B harness: `SINGULARITY_NO_SPAWN_PRIORITY=1` disables all
demotion. Non-darwin falls back to `nice -n 10` (Linux) or no-op — fail-open,
a spawn never breaks because the host lacks a priority tool.

Do NOT demote a backend server process with this (the gateway owns backend
process priority); this is for individual heavy spawns from within a backend
or the CLI.

<!-- AUTOGENERATED:BEGIN — do not edit; regenerated by `./singularity build` -->

## Plugin reference

- Description: OS-priority demotion for background subprocess spawns: backgroundArgv/backgroundPrefix wrap heavy background work (DB forks, agent sessions, builds, worktree checkouts) in darwinbg (taskpolicy -b) so it yields host CPU/IO to the interactive backends.
- Cross-plugin:
  - Imported by: `conversations/runtime-tmux`, `database/admin`, `infra/worktree`
- Server:
  - Exports: Values: `backgroundArgv`, `backgroundPrefix`

<!-- AUTOGENERATED:END -->
