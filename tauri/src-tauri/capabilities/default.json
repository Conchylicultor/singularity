{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. The webview loads the local gateway and uses no Tauri JS APIs, so only core defaults are granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
