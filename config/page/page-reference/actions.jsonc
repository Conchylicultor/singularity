// @hash 18105e49d0bd
// @legend — how to write this file (rewritten by ./singularity build; edits inside this block are lost):
//   Item: "<key>" (terse; <key> is "<pluginId>:<id>")
//   Hide: { "item": "<key>", "hidden": true }
//   Spacer (gap): { "type": "spacer", "id": "<unique-id>" } — grows to push every item after it to the far end of the row (title left, chips right); at most one per slot. Use it instead of a second slot.
//   Group: { "type": "header", "label": "<title>", "collapsed": false, "items": [ "<key>", … ] }
//   Overflow (⋯ menu): { "type": "overflow", "id": "<unique-id>", "items": [ "<key>", … ] }
// The hover-revealed action cluster on a page reference (a sub-page row, a link
// block): a short horizontal strip of icon buttons pinned to the row's right
// edge, read left to right. One action today — open the referenced page in a
// column beside this one. Anything added later belongs after it: opening the
// page is the primary intent, so it stays closest to the row's own click.
{
  "items": [
    "page.page-reference.open-aside:open-aside"
  ]
}
