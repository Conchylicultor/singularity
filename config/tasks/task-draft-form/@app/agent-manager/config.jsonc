// @hash ff4403e504ad
{
  // The agent manager is where tasks are authored, not subject matter — its own
  // URL isn't useful task context, so don't pre-check "URL" there.
  "captureUrlByDefault": false
}
