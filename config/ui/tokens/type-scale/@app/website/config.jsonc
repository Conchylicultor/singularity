// @hash cb1268933cfa
{
  // The site's type scale: a 78px display headline over a 15.5px body on a 1.6
  // line, with every role between sized for a page read at arm's length rather
  // than for chrome. Contributed by the website shell beside the palette; see
  // that file.
  "preset": "equin"
}
