// @hash 6ec84829688d
{
  "views": [
    {
      "id": "tree",
      "name": "Agents",
      // Each sub-agent under the one that spawned it; siblings in launch order,
      // which is how the band reads: the agent started first is at the top.
      "view": {
        "type": "tree",
        "sort": [],
        "visibleFields": ["description", "type", "started"]
      }
    }
  ]
}
