// @hash 2936705f08f1
{
  // Available items in this slot (reorder via the `order` array, hide via `hidden`):
  // Insert a blank gap by adding a "__spacer__<unique-id>" string into the order array.
  // - conversations-recover  — Recovery
  // - events-test  — Events Test
  // - backup  — Backup
  // - claude-cli-calls  — Claude CLI Calls
  // - broadcasts  — Broadcasts
  // - memory  — Memory
  // - crashes  — Crashes
  // - worktree-cleanup  — Worktree Cleanup
  // - logs  — Logs
  // - profiling  — Profiling
  // - queue  — Queue
  "order": [],
  "hidden": []
}
