// @hash 5effd0a7e320
{
  // Landing-page reading order, top to bottom: open with the hero, present the
  // three pillars (the site's narrative spine), prove them with the
  // release-switcher demo (one composition shipping three ways), close with
  // the CTA.
  "items": [
    "apps.website.landing.hero:hero",
    "apps.website.landing.pillars:pillars",
    "apps.website.demos.release-switcher:release-switcher",
    "apps.website.landing.cta:cta"
  ]
}
