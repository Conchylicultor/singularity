// @hash b6c5252e2ba7
{
  // Entries: the Inbox DataView (apps.mail.inbox), the mailbox nav (system views
  // + labels, apps.mail.mailbox), and the Search entry (apps.mail.search).
  // Reorder/hide here — empty `items` keeps the default order.
  // Hide: { "item": "<key>", "hidden": true }. Gap: { "type": "spacer", "id": "<unique-id>" }.
  "items": []
}
