// @hash 201936833d60
{
  // Extra columns contributed into the deployments DataView. One today: the
  // `Release` chip stating what a deploy would pick up.
  "items": [
    "apps.deploy.remote-deploy:release"
  ]
}
