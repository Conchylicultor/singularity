// @hash 2936705f08f1
{
  // Available items in this slot (reorder via the `order` array, hide via `hidden`):
  // - tweakcn  — tweakcn
  // - community-browser  — community-browser
  // - shape  — shape
  // - sidebar-palette  — sidebar-palette
  // - shadow  — shadow
  // - chart  — chart
  // - density  — density
  // - categorical  — categorical
  // - color-palette  — color-palette
  // - typography  — typography
  // - color-adjust  — color-adjust
  "order": [],
  "hidden": []
}
